class Ticket {
  constructor(name, seat, img, created, name, startAt, endAt) {
    this.name = name;
    this.setId = set;
    this.img = img; // 이미지 경로
    this.price = price; // 가격
    this.time = time; // 공연 시간
  }
}

export default Ticket;
